import styled from 'styled-components';
import {
  color,
  space,
  typography,
  layout,
  flexbox,
  border,
  shadow,
} from 'styled-system';

const Box = styled('div')(
  color,
  space,
  typography,
  layout,
  flexbox,
  border,
  shadow
);

export default Box;
